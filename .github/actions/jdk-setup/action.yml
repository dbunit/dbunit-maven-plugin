name: "JDK Setup"
runs:
  using: "composite"
  steps:
    - name: Set up JDK 8
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
