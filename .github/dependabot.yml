version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    open-pull-requests-limit: 10       # Max number of active PRs at once
    groups:
      actions-updates:                 # Group all action updates into one PR
        patterns:
          - "*"
